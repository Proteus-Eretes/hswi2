import { d as defineNuxtCommand, b as cyan, e as commands } from './index.mjs';
import 'tty';
import 'util';
import 'path';
import 'fs';
import 'os';
import 'assert';

const usage = defineNuxtCommand({
  meta: {
    name: "help",
    usage: "nuxt help",
    description: "Show help"
  },
  invoke(_args) {
    const sections = [];
    sections.push(`Usage: ${cyan(`npx nuxi ${Object.keys(commands).join("|")} [args]`)}`);
    console.log(sections.join("\n\n") + "\n");
  }
});

export { usage as default };
